// Export translation resources for type safety
// one-pass only supports en + zh-CN. Other locale files are kept in the
// repo (in case we want them later) but are NOT registered with i18n.
export { default as en } from './en.json';
export { default as zhCN } from './zh-CN.json';
