version: "3.8"

services:
  memcached:
    image: memcached
    restart: always
    expose:
      - "11211"

  onepass:
    image: jhaals/onepass
    restart: always
    ports:
      - "127.0.0.1:80:80"
    environment:
      - MEMCACHED=memcached:11211
      - PORT=80
    healthcheck:
      test: ["CMD", "/onepass-server", "--health-check"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
